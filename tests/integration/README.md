# Integration Coverage

The runnable Rust integration workflow currently lives at `crates/bookmcp-cli/tests/e2e.rs` because this repository is a virtual Cargo workspace. This directory is reserved for cross-crate fixtures and future harnesses that are not tied to a single crate package.
